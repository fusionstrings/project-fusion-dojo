import gulp from 'gulp';
import del from 'del';
import commander from 'commander';
import runSequence from 'run-sequence';
import gulpLoadPlugins from 'gulp-load-plugins';

const $ = gulpLoadPlugins();

gulp.task('dojo-build', () => {
        
});